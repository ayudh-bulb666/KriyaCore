# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
env/

# Database (don't commit the DB — the server provisions its own Postgres)
*.db
instance/

# Locally generated signing key used by deploy/run_public.sh. Committing it
# would put the key that signs session cookies in a public repo.
.secret_key.local

# macOS
.DS_Store

# Bridge: local venv and the offline scan queue
bridge/venv/
bridge/*.db

# Local preview helper: serves instance/demo.db instead of the real database
run_demo.py
